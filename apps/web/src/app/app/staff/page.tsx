export default function StaffHomePage() {
  return (
    <main className="shell">
      <section className="hero">
        <span className="eyebrow">Staff Console</span>
        <h1>Staff queue operations start from this route group.</h1>
        <p>
          The next step after foundation is to add the shared domain schema and then rebuild queue operations here so
          the legacy WPF app can eventually be retired safely.
        </p>
      </section>
    </main>
  )
}
