export default function PatientHomePage() {
  return (
    <main className="shell">
      <section className="hero">
        <span className="eyebrow">Patient Flow</span>
        <h1>Patient routes will land in the next milestones.</h1>
        <p>
          This placeholder keeps the route boundary stable while authentication, site access, and queue check-in are
          built on top of the shared schema and API contracts.
        </p>
      </section>
    </main>
  )
}
